{
	"name": "plugin-mysync",
	"version": "0.1.0",
	"description": "An Obsidian plugin scaffold for syncing with MySync.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin"
	],
	"author": "",
	"license": "MIT",
	"engines": {
		"node": ">=22.22.0"
	},
	"devDependencies": {
		"@types/node": "^22.15.0",
		"esbuild": "0.25.5",
		"tslib": "2.8.1",
		"typescript": "^5.8.3"
	},
	"dependencies": {
		"obsidian": "latest",
		"pouchdb": "^9.0.0"
	}
}
